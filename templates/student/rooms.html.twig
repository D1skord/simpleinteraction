{% extends 'base.html.twig' %}

{% block title %}Rooms{% endblock %}

{% block body %}
  <div class="container">
    <div class="row">
      {% for room in rooms %}
        <div class="col-sm-4 pt-3">
          <div class="card" style="width: 18rem;">
            <div class="card-body">
              <h5 class="card-title"><a href="{{ path('student_room', {'roomId': room.id}) }}">{{ room.name }}</a></h5>
              <div>
                {{ room.description | nl2br }}
              </div>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>



{% endblock %}
