{% extends 'base.html.twig' %}

{% block title %}Rooms{% endblock %}

{% block body %}
  <div class="container">
    <div class="row">

      <div class="col-sm-12">
        <h1 class="mt-4">Редактирование комнаты {{ room.name }}</h1>
      </div>

      <div class="col-sm-12">
        {{ form_start(editRoomForm) }}
        {{ form_row(editRoomForm.name) }}
        {{ form_end(editRoomForm) }}
      </div>
    </div>
  </div>




{% endblock %}
